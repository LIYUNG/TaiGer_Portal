{
    "name": "taiger-portal",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "backend": "cd api&&yarn dev",
        "frontend": "cd client&&yarn start",
        "full": "concurrently \"yarn backend\" \"yarn frontend\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}